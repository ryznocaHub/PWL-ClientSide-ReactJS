import React from 'react'
import { COLORS, FONTS, SIZES } from "../../utils";

export const TiketCreate = ({text}) => {
  return (
    <div>
      <h1 style={{color: COLORS.primary}}>TiketCreate {text}</h1>
    </div>
  )
}
